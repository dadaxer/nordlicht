type: dnf
repos:
    files:
        - netbird.repo
    nonfree: negativo17
    cleanup: true
# remove:
#     auto-remove: true
#     packages:
#         - gnome-tweaks
install:
    packages:
        - netbird
